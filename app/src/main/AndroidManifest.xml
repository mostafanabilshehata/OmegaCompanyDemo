<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.armada.mostafa.omegacompanydemo">
    <uses-sdk
        android:minSdkVersion="4" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/splash_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        </activity>
        <activity android:name=".HomeActivity"
            android:label="   نظام إدارة الموارد البشرية          ">
        </activity>
        <activity android:name=".VacancyInquiryActivity"
            >
        </activity>
        <activity android:name=".VacancyRequestActivity">
        </activity>
        <activity android:name=".SalaryEnquiryActivity">
        </activity>
        <activity android:name=".ViewEmployeeVacationsActivity">
        </activity>

    </application>

</manifest>